import { StyleSheet, Text, View } from "react-native";

const Wallet = () => {
  return (
    <View>
      <Text>Wallet</Text>
    </View>
  );
};

const s = StyleSheet.create({
  
});

export default Wallet;