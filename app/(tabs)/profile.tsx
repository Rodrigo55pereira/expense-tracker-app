import { StyleSheet, Text, View } from "react-native";

const Profile = () => {
  return (
    <View>
      <Text>Profile</Text>
    </View>
  );
};

const s = StyleSheet.create({
  
});

export default Profile;