import { StyleSheet, Text, View } from "react-native";

const Login = () => {
  return (
    <View>
      <Text>Login</Text>
    </View>
  );
};

const s = StyleSheet.create({
  
});

export default Login;